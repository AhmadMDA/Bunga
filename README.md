# Romantic Flowers (React)

Proyek React sederhana bernuansa pink untuk menampilkan bunga romantis.

Instruksi singkat menjalankan lokal:

```bash
npm install
npm run dev
```

Buka `http://localhost:5173` (atau alamat yang ditampilkan oleh Vite).
